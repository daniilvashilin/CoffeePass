import SwiftUI

struct TermsView: View {
    var body: some View {
        ScrollView {
            Text("Terms of Service stub.")
                .frame(maxWidth: .infinity, alignment: .leading)
                .padding()
        }
        .navigationTitle("Terms")
    }
}
