import SwiftUI
struct SupportView: View {
    var body: some View {
        List {
            Section {
                Text("Support stub. Later: FAQ + contact.")
                    .foregroundStyle(.secondary)
            }
        }
        .navigationTitle("Help & Support")
    }
}
