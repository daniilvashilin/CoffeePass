import SwiftUI

struct PolicyView: View {
    var body: some View {
        ScrollView {
            Text("Privacy Policy stub.")
                .frame(maxWidth: .infinity, alignment: .leading)
                .padding()
        }
        .navigationTitle("Privacy Policy")
    }
}

